import axios from "axios";

const API = axios.create({
  // Esto intentará usar la variable de Vercel, y si no existe, usará la de Render directamente
  baseURL: import.meta.env.VITE_API_URL || "https://backendbaguette-1.onrender.com/api/v1",
});

export default API;